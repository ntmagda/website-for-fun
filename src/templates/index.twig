{% extends "layouts/default.twig" %}


{% block body %}
  <header class="header">
    <div class="logo-box">
        <img src="assets/img/logo-white.png" alt="logo" class="logo">
    </div>    
    <div class ="heading">
        <h1 class="heading-primary">
            <span class="heading-primary-main">{{ entry.pageTitle }}</span>
            <span class="heading-primary-sub"> {{entry.pageSubTitle}} </span>
        </h1>
        <a href="#" class="btn btn-white">Discover more</a>
    </div>
</header>
{% endblock %}